# requirements for Streamlit Cloud deployment (UI only)
# The UI just calls the FastAPI backend — no ML deps needed
streamlit==1.45.1
requests==2.32.3
python-dotenv==1.1.0
plotly==5.24.1
fpdf2>=2.7.0
